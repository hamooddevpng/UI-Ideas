(()=>{'use strict';
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;
const fx=document.createElement('style');fx.textContent=`
.hero:before{content:'';position:absolute;inset:0;z-index:3;pointer-events:none;background:radial-gradient(520px circle at var(--mx,72%) var(--my,42%),rgba(74,207,255,.15),transparent 64%);mix-blend-mode:screen}
.hero-canvas{opacity:.95;filter:drop-shadow(0 0 18px rgba(78,211,255,.28))}
.hero-panel{background:linear-gradient(150deg,rgba(5,50,78,.48),rgba(2,24,39,.58))!important;border-color:rgba(124,224,255,.24)!important;box-shadow:0 30px 90px rgba(0,0,0,.34),0 0 55px rgba(45,185,238,.09)!important}
.hero-panel:after{content:'3D NETWORK';position:absolute;right:18px;top:16px;font-size:9px;letter-spacing:.12em;color:#7fe0ff;font-weight:800;opacity:.85}.hero-panel{position:relative}
.signal{animation:signalBreath 4.2s ease-in-out infinite;animation-delay:var(--signal-delay,0s)}
@keyframes signalBreath{0%,100%{background:transparent}50%{background:rgba(95,215,255,.045)}}
.gateway{transform-style:preserve-3d;will-change:transform}.gateway .round-arrow,.gateway .label,.gateway-bottom{transform:translateZ(18px)}
.plan.featured{position:relative;overflow:hidden}.plan.featured:after{content:'';position:absolute;width:180px;height:180px;border-radius:50%;background:radial-gradient(circle,rgba(112,221,255,.22),transparent 68%);right:-65px;top:-65px;animation:floatGlow 5s ease-in-out infinite}@keyframes floatGlow{50%{transform:translate(-20px,18px) scale(1.15)}}
.rail-card.dark{background:linear-gradient(145deg,#03263e,#07537d)!important}.rail-card{transition:transform .35s ease,box-shadow .35s ease}.rail-card:hover{transform:translateY(-8px) scale(1.015);box-shadow:0 28px 65px rgba(4,48,75,.15)}
@media(max-width:900px){.hero-canvas{opacity:.52}.hero:before{display:none}}
`;document.head.appendChild(fx);
const nav=document.querySelector('.nav');addEventListener('scroll',()=>nav&&nav.classList.toggle('scrolled',scrollY>40),{passive:true});
const slides=[...document.querySelectorAll('.hero-bg')],dots=[...document.querySelectorAll('.hero-dot')];let slideIndex=0;function showSlide(i){slideIndex=(i+slides.length)%slides.length;slides.forEach((s,n)=>s.classList.toggle('active',n===slideIndex));dots.forEach((d,n)=>d.classList.toggle('active',n===slideIndex));}dots.forEach((d,i)=>d.addEventListener('click',()=>showSlide(i)));if(!reduce&&slides.length)setInterval(()=>showSlide(slideIndex+1),5200);
const reveal=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}}),{threshold:.12});reveal.forEach(el=>io.observe(el));}else reveal.forEach(el=>el.classList.add('in'));
const story=document.getElementById('storyScene'),storyImg=story&&story.querySelector('img');function storyParallax(){if(!story||!storyImg||reduce||innerWidth<721)return;const r=story.getBoundingClientRect(),p=Math.max(0,Math.min(1,(innerHeight-r.top)/(innerHeight+r.height)));storyImg.style.transform=`translate3d(0,${(p-.5)*36}px,0) scale(1.04)`;}addEventListener('scroll',storyParallax,{passive:true});
const shell=document.getElementById('railShell'),rail=document.getElementById('rail'),progress=document.getElementById('railProgress');let railX=0;function maxRail(){return rail?Math.max(0,rail.scrollWidth-innerWidth+90):0}function paintRail(){if(!rail)return;if(innerWidth<=1100){rail.style.transform='';return}const max=maxRail();railX=Math.max(0,Math.min(max,railX));rail.style.transform=`translate3d(${-railX}px,0,0)`;if(progress)progress.style.width=(max?railX/max*100:0)+'%'}function railActive(){if(!shell||innerWidth<=1100)return false;const r=shell.getBoundingClientRect();return r.top<innerHeight*.72&&r.bottom>innerHeight*.28}addEventListener('wheel',e=>{if(!railActive())return;const max=maxRail();if(max<=0)return;const forward=e.deltaY>0,back=e.deltaY<0,canForward=railX<max-1,canBack=railX>1;if((forward&&canForward)||(back&&canBack)){e.preventDefault();railX+=e.deltaY*1.05;paintRail()}},{passive:false});addEventListener('resize',paintRail);paintRail();
const hero=document.querySelector('.hero');if(hero&&!reduce){hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect();hero.style.setProperty('--mx',`${e.clientX-r.left}px`);hero.style.setProperty('--my',`${e.clientY-r.top}px`);});}
if(!reduce&&matchMedia('(pointer:fine)').matches){document.querySelectorAll('.gateway').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*4).toFixed(2)}deg) rotateY(${(x*5).toFixed(2)}deg) translateY(-5px)`;});card.addEventListener('pointerleave',()=>card.style.transform='');});}
document.querySelectorAll('.signal').forEach((el,i)=>el.style.setProperty('--signal-delay',`${i*.22}s`));
addEventListener('load',()=>{try{if(!window.THREE||reduce)return;const canvas=document.getElementById('heroCanvas');if(!canvas)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(42,innerWidth/innerHeight,.1,100),renderer=new THREE.WebGLRenderer({canvas,alpha:true,antialias:innerWidth>800});camera.position.z=8.2;renderer.setPixelRatio(Math.min(devicePixelRatio,innerWidth<720?1:1.65));renderer.setSize(innerWidth,innerHeight);const globe=new THREE.Group();scene.add(globe);globe.add(new THREE.Mesh(new THREE.SphereGeometry(2.05,48,48),new THREE.MeshBasicMaterial({color:0x0d72a8,transparent:true,opacity:.12})));const wire=new THREE.Mesh(new THREE.SphereGeometry(2.12,32,32),new THREE.MeshBasicMaterial({color:0x63d9ff,transparent:true,opacity:.18,wireframe:true}));globe.add(wire);const pts=new THREE.Points(new THREE.SphereGeometry(2.18,72,72),new THREE.PointsMaterial({color:0xa9efff,size:.03,transparent:true,opacity:.8}));globe.add(pts);[2.22,2.30].forEach((r,i)=>globe.add(new THREE.Mesh(new THREE.SphereGeometry(r,42,42),new THREE.MeshBasicMaterial({color:i?0x137fc1:0x59d7ff,transparent:true,opacity:i?.04:.055,side:THREE.BackSide}))));[[2.75,0,0],[2.65,.72,.15],[2.55,-.55,.7]].forEach(([r,rx,rz])=>{const ring=new THREE.Mesh(new THREE.TorusGeometry(r,.009,8,180),new THREE.MeshBasicMaterial({color:0x75ddff,transparent:true,opacity:.32}));ring.rotation.x=Math.PI/2+rx;ring.rotation.z=rz;globe.add(ring);});const positions=[];for(let i=0;i<16;i++){const a=Math.random()*Math.PI*2,b=(Math.random()-.5)*Math.PI*.9,p=new THREE.Vector3(2.19*Math.cos(b)*Math.cos(a),2.19*Math.sin(b),2.19*Math.cos(b)*Math.sin(a));positions.push(p);const node=new THREE.Mesh(new THREE.SphereGeometry(.038,12,12),new THREE.MeshBasicMaterial({color:0xc9f6ff}));node.position.copy(p);globe.add(node);const halo=new THREE.Mesh(new THREE.SphereGeometry(.10,12,12),new THREE.MeshBasicMaterial({color:0x2ebeff,transparent:true,opacity:.20}));halo.position.copy(p);globe.add(halo);}for(let i=0;i<14;i++){const p1=positions[i%positions.length],p2=positions[(i*5+3)%positions.length],mid=p1.clone().add(p2).multiplyScalar(.5).normalize().multiplyScalar(3.25+Math.random()*.35),curve=new THREE.QuadraticBezierCurve3(p1,mid,p2);globe.add(new THREE.Line(new THREE.BufferGeometry().setFromPoints(curve.getPoints(48)),new THREE.LineBasicMaterial({color:0x73e2ff,transparent:true,opacity:.48})));}const satellites=[];for(let i=0;i<3;i++){const s=new THREE.Mesh(new THREE.OctahedronGeometry(.09),new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.9}));s.userData={angle:i*2.1,speed:.0035+i*.0008,radius:2.72+i*.12,tilt:(i-1)*.38};globe.add(s);satellites.push(s);}globe.position.x=innerWidth<900?1.65:2.65;let tx=0,ty=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*.42;ty=(e.clientY/innerHeight-.5)*.24},{passive:true});function render(){globe.rotation.y+=.0017;globe.rotation.x+=(ty-globe.rotation.x)*.025;globe.rotation.z+=(tx*.2-globe.rotation.z)*.025;wire.rotation.y-=.0011;pts.rotation.y+=.0006;satellites.forEach(s=>{s.userData.angle+=s.userData.speed*10;const a=s.userData.angle,r=s.userData.radius,t=s.userData.tilt;s.position.set(Math.cos(a)*r,Math.sin(a)*r*Math.cos(t),Math.sin(a)*r*Math.sin(t));s.rotation.x+=.03;s.rotation.y+=.025;});renderer.render(scene,camera);requestAnimationFrame(render)}render();addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight);globe.position.x=innerWidth<900?1.65:2.65});}catch(err){console.warn('3D layer skipped:',err)}});
if(!document.querySelector('script[data-audience-map]')){const audience=document.createElement('script');audience.src='assets/audience-map.js';audience.defer=true;audience.dataset.audienceMap='1';document.body.appendChild(audience)}
if(!document.querySelector('script[src$="final-safety.js"]')){const safety=document.createElement('script');safety.src='assets/final-safety.js';safety.defer=true;document.body.appendChild(safety)}
})();