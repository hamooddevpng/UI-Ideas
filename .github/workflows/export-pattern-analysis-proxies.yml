name: Export Pattern Analysis Proxies

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary helper disabled; pattern analysis now uses committed JSON manifests."
