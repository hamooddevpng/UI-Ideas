name: Design 42 Starlink Specificity Apply

on:
  push:
    branches:
      - refactor/design42-css-cleanup
    paths:
      - '.github/workflows/design42-starlink-specificity-apply.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Remove Starlink specificity overrides
        run: |
          python3 <<'PY'
          from pathlib import Path

          path = Path('assets/design42/design42-core.css')
          text = path.read_text()
          start_marker = '/* Design 42: Starlink hardware refresh only. Banner composition and copy intentionally untouched. */'
          end_marker = '/* Design 42: static province laser network v6 */'
          start = text.index(start_marker)
          end = text.index(end_marker, start + len(start_marker))
          section = text[start:end]
          count = section.count('!important')
          if count != 19:
              raise SystemExit(f'Expected 19 Starlink !important declarations, found {count}.')
          section = section.replace('!important', '')
          text = text[:start] + section + text[end:]
          path.write_text(text)
          print('Removed 19 Starlink !important declarations.')
          PY

          node tests/design42-css-architecture.test.mjs

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add assets/design42/design42-core.css
          git commit -m 'refactor: remove Starlink important overrides'
          git push origin HEAD:refactor/design42-css-cleanup
