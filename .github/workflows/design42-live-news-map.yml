name: One-time Design 42 live news and hero map
on:
  push:
    paths:
      - '.github/workflows/design42-live-news-map.yml'
permissions:
  contents: write
jobs:
  patch-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply Design 42 live news and hero map patch
        run: python3 .github/scripts/design42_live_news_map.py
      - name: JavaScript syntax check
        run: |
          python3 - <<'PY'
          import re,pathlib
          s=pathlib.Path('42.html').read_text()
          pathlib.Path('/tmp/42.js').write_text('\n'.join(re.findall(r'<script>(.*?)</script>',s,re.S)))
          PY
          node --check /tmp/42.js
      - name: Browser interaction test
        run: |
          npm install --prefix /tmp/design42-pw --no-save playwright-core@1.55.0 >/dev/null 2>&1
          python3 -m http.server 8000 >/tmp/42-http.log 2>&1 &
          NODE_PATH=/tmp/design42-pw/node_modules node .github/scripts/verify42_live_news_map.js
      - name: Commit verified changes
        run: |
          rm -f .github/workflows/design42-live-news-map.yml \
                .github/scripts/design42_live_news_map.py \
                .github/scripts/verify42_live_news_map.js
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add 42.html .github/workflows/design42-live-news-map.yml .github/scripts/design42_live_news_map.py .github/scripts/verify42_live_news_map.js
          git commit -m 'Add live news rotation and restore hero PNG map'
          for try in 1 2 3; do
            if git push origin HEAD:main; then exit 0; fi
            git fetch origin main
            git rebase origin/main
          done
          exit 1
